Thunderous Assistant v10.0.0 - Planned Features & Ideas
Upcoming Enhancements

Live clock on main dashboard
All data kept locally for privacy

Issues to Fix

Discord rich presence currently broken
Update rich presence tracking:
Prioritize video games (e.g. "slaying dragons in World of Warcraft", "exploring Eorzea in Final Fantasy")
If no game or Spotify active: show current module (e.g. "getting dues done")


Code Refactor

Refactor main.py to only load external-facing modules by default
Internal modules stay local, never pushed to GitHub
Add command-line switch to load internal modules (e.g. --internal)

Internal Modules (Local-only)

NSFW Reddit Viewer
Pulls top daily posts from selected subs, clean list, no audio
Private Trainer
Voice-guided solo session. Customizable speed & style.
AI needs to handle voice synthesis, timing, user input
Image Forge
Local text-to-image generator using Stable Diffusion
One-prompt interface, no data leaves the machine

Local Stable Diffusion Setup

Pull repo: git clone https://github.com/Stability-AI/stablediffusion
Install: pip install torch torchvision (use CUDA if Nvidia)
Run: launch the web UI or wrap it in a Python function
Integrate: prompt goes straight from module → model → image, no filter, no upload